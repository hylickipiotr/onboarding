export const getFormFieldDescriptionId = (name: string) => {
  return `${name}-description`;
};
export const getFormFieldErrorMessageId = (name: string) => {
  return `${name}-error-message`;
};
export const getFormFieldLabelId = (name: string) => {
  return `${name}-label`;
};
